# Example: dog-day

Real, regenerable output (`make examples`). `prompt.txt` is what was typed; `message.txt` the chat replies;
`report.html` the self-contained report (open it on your phone); `route.gpx` the export for swisstopo;
`audit.json` the structured audit behind it. Conditions are those of the generation date printed in the report.
